---
title: "Creating OAuth Sessions"
description: "Create OAuth sessions for user authorization"
---

Create OAuth sessions for user authorization.

## Creating a Session

<CodeGroup>
```typescript TypeScript
let oauthSession = await metorial.oauth.sessions.create({
  serverDeploymentId: 'slack-deployment-id'
});

console.log('Authorize:', oauthSession.url);
```

```python Python
oauth_session = metorial.oauth.sessions.create(
    server_deployment_id="slack-deployment-id"
)

print(f"Authorize: {oauth_session.url}")
```
</CodeGroup>
